import frameworkDecorator from "@nl-rvo/storybook/frameworkDecorator";
import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { argTypes, defaultArgs, Heading } from "./template";

<Meta
  title="CSS Components/Heading"
  argTypes={argTypes}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
  decorators={[frameworkDecorator]}
/>

# Heading

<Canvas>
  <Story name="Heading" args={{ ...defaultArgs, type: "h1", distanced: false, textContent: "Heading" }}>
    {Heading.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Heading" />
