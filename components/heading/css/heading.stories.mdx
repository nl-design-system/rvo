import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { argTypes, Heading, defaultArgs } from "./template";

<Meta
  title="CSS Components/Heading"
  argTypes={argTypes}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Heading

<Canvas>
  <Story name="Heading" args={{ ...defaultArgs, distanced: false, textContent: "Heading" }}>
    {Heading.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Heading" />
