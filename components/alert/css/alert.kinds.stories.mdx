import { Canvas, Meta, Story } from "@storybook/addon-docs";
import { Alert, argTypes } from "./template";
import { defaultArgs } from "./defaultArgs";

<Meta
  title="CSS Components/Alert/Kinds"
  component={Alert}
  argTypes={argTypes}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Alert kinds

## Info

<Canvas>
  <Story name="Info" args={{ ...defaultArgs, content: "This is an example of an info alert", kind: "info" }}>
    {Alert.bind({})}
  </Story>
</Canvas>

## Warning

<Canvas>
  <Story name="Warning" args={{ ...defaultArgs, content: "This is an example of a warning alert", kind: "warning" }}>
    {Alert.bind({})}
  </Story>
</Canvas>

## Error

<Canvas>
  <Story name="Error" args={{ ...defaultArgs, content: "This is an example of an error alert", kind: "error" }}>
    {Alert.bind({})}
  </Story>
</Canvas>

## Success

<Canvas>
  <Story name="Success" args={{ ...defaultArgs, content: "This is an example of a success alert", kind: "success" }}>
    {Alert.bind({})}
  </Story>
</Canvas>
