import frameworkDecorator from "@nl-rvo/storybook/frameworkDecorator";
import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { Alert, argTypes, defaultArgs } from "./template";
import "./index.scss";

<Meta
  title="CSS Components/Alert/Alert"
  argTypes={argTypes}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
  decorators={[frameworkDecorator]}
/>

# Alert

<Canvas>
  <Story name="Alert" args={defaultArgs}>
    {Alert.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Alert" />
