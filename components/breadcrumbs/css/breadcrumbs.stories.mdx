import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { defaultArgs } from "./defaultArgs";
import { argTypes, Breadcrumbs } from "./template";

<Meta
  title="CSS Components/Breadcrumbs/Breadcrumbs"
  component={Breadcrumbs}
  argTypes={argTypes}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Breadcrumbs

<Canvas>
  <Story name="Breadcrumbs" args={defaultArgs}>
    {Breadcrumbs.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Breadcrumbs" />

## Dependencies

@nl-rvo/assets/icons
