import { argTypes, IIconProps } from './template';

export const defaultArgs: IIconProps = {
  // TODO: Fix this
  // icon: argTypes.icon.options[0],
  icon: 'pijltje_omlaag',
  size: argTypes.size.options[2],
  color: argTypes.color.options[0],
};
