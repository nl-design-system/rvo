import { Canvas, Controls, Meta, Title } from "@storybook/blocks";
import changeLog from "./CHANGELOG.md";
import documentation from "./README.md";
import * as FooterStories from "./footer.stories";
import { Changelog, Readme } from "../../packages/storybook/config/storybook-blocks";

<Meta of={FooterStories} name="Documentatie" />

<Title />
Items containing a link will be rendered as text links with an icon (e.g. > Link content), otherwise items are rendered as
generic HTML content.
<Canvas of={FooterStories.Default} layout="padded" />
<Controls />

## Documentatie

<Readme markdown={documentation} />

## Changelog

<Changelog markdown={changeLog} />

## Dependencies

_@nl-rvo/assets/icons_
