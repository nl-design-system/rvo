import { ArgsTable, Canvas, Description, Meta, Story } from "@storybook/blocks";
import changeLog from "./CHANGELOG.md";
import { defaultArgs } from "./defaultArgs";
import { argTypes, Footer } from "./template";
import * as FooterStories from "./footer.stories";

<Meta of={FooterStories} />

# Footer

<Canvas>
  <Story of={FooterStories.Footer} />
</Canvas>

<ArgsTable story="Footer" />

Items containing a link will be rendered as text links with an icon (e.g. > Link content), otherwise items are rendered as generic HTML content.

## Dependencies

@nl-rvo/assets/icons

## Changelog

<Description>{changeLog}</Description>
