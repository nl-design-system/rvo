import { ArgsTable, Canvas, Description, Meta, Story } from "@storybook/addon-docs";
import changeLog from "./CHANGELOG.md";
import { defaultArgs } from "./defaultArgs";
import { argTypes, Checkbox } from "./template";

<Meta
  title="Componenten/Form/Checkbox/Checkbox"
  component={Checkbox}
  argTypes={argTypes}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
    docusaurus: {
      link: "form-checkbox",
    },
  }}
/>

# Form checkbox

<Canvas>
  <Story name="Checkbox" args={defaultArgs}>
    {Checkbox.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Checkbox" />

## Changelog

<Description>{changeLog}</Description>
