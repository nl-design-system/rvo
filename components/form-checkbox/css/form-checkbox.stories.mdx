import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { argTypes, defaultArgs, Checkbox } from "./template";

<Meta
  title="CSS Components/Forms/Checkbox/Checkbox"
  argTypes={argTypes}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Form checkbox

<Canvas>
  <Story name="Checkbox" args={defaultArgs}>
    {Checkbox.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Checkbox" />
