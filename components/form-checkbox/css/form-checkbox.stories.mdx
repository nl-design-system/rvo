import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { argTypes, Checkbox, defaultArgs } from "./template";
import frameworkDecorator from "@nl-rvo/storybook/frameworkDecorator";

<Meta
  title="CSS Components/Form/Checkbox/Checkbox"
  argTypes={argTypes}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
  decorators={[frameworkDecorator]}
/>

# Form checkbox

<Canvas>
  <Story name="Checkbox" args={defaultArgs}>
    {Checkbox.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Checkbox" />
