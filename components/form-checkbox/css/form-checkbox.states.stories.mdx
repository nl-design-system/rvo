import { Canvas, Meta, Story } from "@storybook/addon-docs";
import { argTypes, Checkbox, defaultArgs } from "./template";

<Meta
  title="CSS Components/Form/Checkbox/States"
  component={Checkbox}
  argTypes={argTypes}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

## Hover

<Canvas>
  <Story name="Hover" args={{ ...defaultArgs, hover: true }}>
    {Checkbox.bind({})}
  </Story>
</Canvas>

## Checked

<Canvas>
  <Story name="Checked" args={{ ...defaultArgs, checked: true }}>
    {Checkbox.bind({})}
  </Story>
</Canvas>

## Indeterminate

<Canvas>
  <Story name="Indeterminate" args={{ ...defaultArgs, checked: true, indeterminate: true }}>
    {Checkbox.bind({})}
  </Story>
</Canvas>

## Disabled

<Canvas>
  <Story name="Disabled" args={{ ...defaultArgs, disabled: true }}>
    {Checkbox.bind({})}
  </Story>
</Canvas>

## Active

<Canvas>
  <Story name="Active" args={{ ...defaultArgs, active: true }}>
    {Checkbox.bind({})}
  </Story>
</Canvas>

## Focus

<Canvas>
  <Story name="Focus" args={{ ...defaultArgs, focus: true }}>
    {Checkbox.bind({})}
  </Story>
</Canvas>
