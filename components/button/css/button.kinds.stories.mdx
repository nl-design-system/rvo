import { Canvas, Meta, Story } from "@storybook/addon-docs";
import { argTypes, Button } from "./template";
import { defaultArgs } from "./defaultArgs";

<Meta
  title="CSS Components/Button/Kinds"
  component={Button}
  argTypes={argTypes}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Button kinds

## Primary

<Canvas>
  <Story name="Primary" args={{ ...defaultArgs, kind: "primary" }}>
    {Button.bind({})}
  </Story>
</Canvas>

## Secondary

<Canvas>
  <Story
    name="Secondary"
    args={{
      ...defaultArgs,
      kind: "secondary",
    }}
  >
    {Button.bind({})}
  </Story>
</Canvas>

## Tertiary

<Canvas>
  <Story
    name="Tertiary"
    args={{
      ...defaultArgs,
      kind: "tertiary",
    }}
  >
    {Button.bind({})}
  </Story>
</Canvas>

## Quaternary

<Canvas>
  <Story
    name="Quaternary"
    args={{
      ...defaultArgs,
      kind: "quaternary",
    }}
  >
    {Button.bind({})}
  </Story>
</Canvas>

## Warning subtle

<Canvas>
  <Story
    name="Warning subtle"
    args={{
      ...defaultArgs,
      kind: "warning-subtle",
    }}
  >
    {Button.bind({})}
  </Story>
</Canvas>

## Warning

<Canvas>
  <Story
    name="Warning"
    args={{
      ...defaultArgs,
      kind: "warning",
    }}
  >
    {Button.bind({})}
  </Story>
</Canvas>
