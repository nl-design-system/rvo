import { Canvas, Meta, Story } from "@storybook/addon-docs";
import { AllButtonKinds, argTypes } from "./template";
import "./index.scss";

<Meta
  title="CSS Components/Button/Icons"
  argTypes={argTypes}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Button icons

Button containing an icon aligned before:

<Canvas>
  <Story
    name="With icon before"
    args={{
      kind: "primary",
      showIcon: "before",
    }}
  >
    {AllButtonKinds.bind({})}
  </Story>
</Canvas>

Button containing an icon aligned after:

<Canvas>
  <Story
    name="With icon after"
    args={{
      kind: "primary",
      showIcon: "after",
    }}
  >
    {AllButtonKinds.bind({})}
  </Story>
</Canvas>
