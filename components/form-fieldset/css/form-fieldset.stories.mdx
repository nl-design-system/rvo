import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { argTypes, defaultArgs, Fieldset } from "./template";
import "./index.scss";
import frameworkDecorator from "@nl-rvo/storybook/frameworkDecorator";

<Meta
  title="CSS Components/Form/Fieldset"
  argTypes={argTypes}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
  decorators={[frameworkDecorator]}
/>

# Fieldset

<Canvas>
  <Story name="Fieldset" args={defaultArgs}>
    {Fieldset.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Fieldset" />
