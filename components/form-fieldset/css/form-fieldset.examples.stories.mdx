import { Canvas, Meta, Story } from "@storybook/addon-docs";
import { argTypes, defaultArgs, Fieldset } from "./template";
import "./index.scss";

<Meta
  title="CSS Components/Form/Fieldset/Voorbeelden"
  argTypes={argTypes}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Fieldset met fields

<Canvas>
  <Story name="Fieldset met fields" args={{ ...defaultArgs, withFields: true }}>
    {Fieldset.bind({})}
  </Story>
</Canvas>
