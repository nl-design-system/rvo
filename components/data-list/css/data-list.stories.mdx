import frameworkDecorator from "@nl-rvo/storybook/frameworkDecorator";
import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { argTypes, DataList, defaultArgs } from "./template";
import "./index.scss";

<Meta
  title="CSS Components/Data list/Data list"
  argTypes={argTypes}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
  decorators={[frameworkDecorator]}
/>

# Data list

<Canvas>
  <Story name="Data list" args={defaultArgs}>
    {DataList.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Data list" />
