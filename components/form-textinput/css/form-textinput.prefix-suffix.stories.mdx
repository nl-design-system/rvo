import { Canvas, Meta, Story } from "@storybook/addon-docs";
import { defaultArgs } from "./defaultArgs";
import { argTypes, TextInput } from "./template";

<Meta
  title={`CSS Components/Form/Text input/Prefix & Suffix`}
  component={TextInput}
  argTypes={argTypes}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Prefix

<Canvas>
  <Story name="Invalid" args={{ ...defaultArgs, valueType: "currency", prefix: "€" }}>
    {TextInput.bind({})}
  </Story>
</Canvas>

## Suffix

<Canvas>
  <Story name="Focus" args={{ ...defaultArgs, valueType: "number", suffix: "m³" }}>
    {TextInput.bind({})}
  </Story>
</Canvas>
