import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { defaultArgs } from "./defaultArgs";
import { argTypes, TextInput } from "./template";

<Meta
  title="CSS Components/Form/Text input"
  component={TextInput}
  argTypes={argTypes}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Form text input

<Canvas>
  <Story name="Text input" args={defaultArgs}>
    {TextInput.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Text input" />
