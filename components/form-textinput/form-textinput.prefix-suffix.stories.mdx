import { Canvas, Meta, Story } from "@storybook/addon-docs";
import { defaultArgs } from "./defaultArgs";
import { argTypes, TextInput } from "./template";

<Meta
  title={`Componenten/Form/Text input/Prefix & Suffix`}
  component={TextInput}
  argTypes={argTypes}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
    docusaurus: {
      link: "form-textinput",
    },
  }}
/>

# Prefix

<Canvas>
  <Story name="Prefix" args={{ ...defaultArgs, valueType: "currency", prefix: "€" }}>
    {TextInput.bind({})}
  </Story>
</Canvas>

## Suffix

<Canvas>
  <Story name="Suffix" args={{ ...defaultArgs, valueType: "number", suffix: "m³" }}>
    {TextInput.bind({})}
  </Story>
</Canvas>
