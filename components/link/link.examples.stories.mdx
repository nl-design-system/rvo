import { Canvas, Meta, Story } from "@storybook/addon-docs";
import { defaultArgs } from "./defaultArgs";
import { argTypes, Link } from "./template";

<Meta
  title="CSS Components/Link/Voorbeelden"
  component={Link}
  argTypes={argTypes}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Terug link

<Canvas>
  <Story
    name="Terug link"
    args={{ ...defaultArgs, content: "Terug", icon: "terug", showIcon: "before", iconAriaLabel: "Ga terug" }}
  >
    {Link.bind({})}
  </Story>
</Canvas>

# Verder link

<Canvas>
  <Story
    name="Verder link"
    args={{
      ...defaultArgs,
      content: "Verder",
      icon: "pijl-naar-rechts",
      showIcon: "after",
      iconAriaLabel: "Ga verder",
    }}
  >
    {Link.bind({})}
  </Story>
</Canvas>

# Externe link

<Canvas>
  <Story
    name="Externe link"
    args={{
      ...defaultArgs,
      content: "Externe link",
      icon: "pijl-naar-rechtsboven",
      showIcon: "after",
      iconAriaLabel: "Ga naar www.rvo.nl (externe link)",
    }}
  >
    {Link.bind({})}
  </Story>
</Canvas>

# Zonder underline

Let op: Gebruik bij een link zonder onderstreping altijd een icoon om duidelijk te maken dat het om een link gaat.

<Canvas>
  <Story
    name="Zonder underline"
    args={{
      ...defaultArgs,
      content: "Verder",
      icon: "pijl-naar-rechts",
      showIcon: "after",
      noUnderline: true,
      iconAriaLabel: "Ga verder",
    }}
  >
    {Link.bind({})}
  </Story>
</Canvas>

# Small icon

<Canvas>
  <Story
    name="Small icon"
    args={{
      ...defaultArgs,
      content: "Verder",
      icon: "pijl-naar-rechts",
      showIcon: "after",
      iconSize: "sm",
      iconAriaLabel: "Ga verder",
    }}
  >
    {Link.bind({})}
  </Story>
</Canvas>
