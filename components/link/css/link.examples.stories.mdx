import { Canvas, Meta, Story } from "@storybook/addon-docs";
import { argTypes, defaultArgs, Link } from "./template";
import "./index.scss";

<Meta
  title="CSS Components/Link/Voorbeelden"
  component={Link}
  argTypes={argTypes}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Terug link

<Canvas>
  <Story name="Terug link" args={{ ...defaultArgs, content: "Terug", icon: "terug", showIcon: "before" }}>
    {Link.bind({})}
  </Story>
</Canvas>

# Verder link

<Canvas>
  <Story name="Verder link" args={{ ...defaultArgs, content: "Verder", icon: "pijl-naar-rechts", showIcon: "after" }}>
    {Link.bind({})}
  </Story>
</Canvas>

# Externe link

<Canvas>
  <Story
    name="Externe link"
    args={{ ...defaultArgs, content: "Externe link", icon: "pijl-naar-rechtsboven", showIcon: "after" }}
  >
    {Link.bind({})}
  </Story>
</Canvas>

# Zonder underline

<Canvas>
  <Story
    name="Zonder underline"
    args={{ ...defaultArgs, content: "Verder", icon: "pijl-naar-rechts", showIcon: "after", noUnderline: true }}
  >
    {Link.bind({})}
  </Story>
</Canvas>

# Small icon

<Canvas>
  <Story
    name="Small icon"
    args={{ ...defaultArgs, content: "Verder", icon: "pijl-naar-rechts", showIcon: "after", iconSize: "sm" }}
  >
    {Link.bind({})}
  </Story>
</Canvas>
