import { ArgsTable, Canvas, Description, Meta, Story } from "@storybook/addon-docs";
import { argTypes, defaultArgs, SkipLink } from "./template";
import changeLog from "../CHANGELOG.md";

<Meta
  title="CSS Components/Skip link/Skip link"
  component={SkipLink}
  argTypes={argTypes}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

<Canvas>
  <Story name="Skip link" args={defaultArgs}>
    {SkipLink.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Skip link" />

Press TAB to focus and show the skip link button.

## Changelog

<Description>{changeLog}</Description>
