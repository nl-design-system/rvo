import frameworkDecorator from "@nl-rvo/storybook/frameworkDecorator";
import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { argTypes, defaultArgs, ExpandableText } from "./template";
import "./index.scss";

<Meta
  title="CSS Components/Expandable text/Expandable text"
  argTypes={argTypes}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
  decorators={[frameworkDecorator]}
/>

# Expandable text

<Canvas>
  <Story name="Expandable text" args={defaultArgs}>
    {ExpandableText.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Expandable text" />

## Dependencies

@nl-rvo/assets/icons
