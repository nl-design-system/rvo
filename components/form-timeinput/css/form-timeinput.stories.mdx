import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { argTypes, TimeInput, defaultArgs } from "./template";

<Meta
  title="CSS Components/Form/Time input/Time input"
  component={TimeInput}
  argTypes={argTypes}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Form time input

<Canvas>
  <Story name="Time input" args={defaultArgs}>
    {TimeInput.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Time input" />
