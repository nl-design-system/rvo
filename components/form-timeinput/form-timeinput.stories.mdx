import { ArgsTable, Canvas, Description, Meta, Story } from "@storybook/addon-docs";
import changeLog from "./CHANGELOG.md";
import { defaultArgs } from "./defaultArgs";
import { argTypes, TimeInput } from "./template";

<Meta
  title="Componenten/Form/Time input/Time input"
  component={TimeInput}
  argTypes={argTypes}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
    docusaurus: {
      link: "form-timeinput",
    },
  }}
/>

# Time input

<Canvas>
  <Story name="Time input" args={defaultArgs}>
    {TimeInput.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Time input" />

## Changelog

<Description>{changeLog}</Description>
