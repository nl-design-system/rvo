import frameworkDecorator from "@nl-rvo/storybook/frameworkDecorator";
import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { argTypes, defaultArgs, FormFeedback } from "./template";

<Meta
  title="CSS Components/Form/Form feedback"
  argTypes={argTypes}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
  decorators={[frameworkDecorator]}
/>

# Form feedback

<Canvas>
  <Story name="Form feedback" args={defaultArgs}>
    {FormFeedback.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Form feedback" />
