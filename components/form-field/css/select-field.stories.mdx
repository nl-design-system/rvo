import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { argTypes, defaultArgs, SelectField } from "./select-field.template";
import "./index.scss";
import frameworkDecorator from "@nl-rvo/storybook/frameworkDecorator";

<Meta
  title="CSS Components/Form/Field/Select field"
  argTypes={argTypes}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
  decorators={[frameworkDecorator]}
/>

# Select field

<Canvas>
  <Story name="Select field" args={defaultArgs}>
    {SelectField.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Select field" />
