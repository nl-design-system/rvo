import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { argTypes, defaultArgs, FileInputField } from "./fileinput-field.template";
import "./index.scss";

<Meta
  title="CSS Components/Form/Field/File input field"
  component={FileInputField}
  argTypes={argTypes}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# File input field

<Canvas>
  <Story name="File input field" args={defaultArgs}>
    {FileInputField.bind({})}
  </Story>
</Canvas>

<ArgsTable story="File input field" />
