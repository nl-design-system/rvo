import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { argTypes, defaultArgs, FileInput } from "./template";

<Meta
  title="CSS Components/Form/File input"
  component={FileInput}
  argTypes={argTypes}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Form file input

<Canvas>
  <Story name="File input" args={defaultArgs}>
    {FileInput.bind({})}
  </Story>
</Canvas>

<ArgsTable story="File input" />
