import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { defaultArgs } from "./defaultArgs";
import { argTypes, LayoutColumnRow } from "./template";

<Meta
  title="Componenten/Layout/Layout column & row"
  component={LayoutColumnRow}
  argTypes={argTypes}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
    docusaurus: {
      link: "layout-column-row",
    },
  }}
/>

<Canvas>
  <Story name="Row align to top" args={{ ...defaultArgs, row: true, alignToTop: true }}>
    {LayoutColumnRow.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Row align to top" />
