import frameworkDecorator from "@nl-rvo/storybook/frameworkDecorator";
import { ArgsTable, Canvas, Description, Meta, Story } from "@storybook/addon-docs";
import { argTypes, defaultArgs, LayoutColumnRow } from "./template";
import "./index.scss";
import markdown from "../README.md";

<Meta
  title="CSS Components/Layout/Layout column &frasl; row"
  argTypes={argTypes}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
  decorators={[frameworkDecorator]}
/>

<Description>{markdown}</Description>

<Canvas>
  <Story name="Layout column &frasl; row" args={defaultArgs}>
    {LayoutColumnRow.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Layout column &frasl; row" />
