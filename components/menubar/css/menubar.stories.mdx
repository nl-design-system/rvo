import { ArgsTable, Canvas, Description, Meta, Story } from "@storybook/addon-docs";
import { defaultArgs } from "./defaultArgs";
import { argTypes, MenuBar } from "./template";
import changeLog from "../CHANGELOG.md";

<Meta
  title="CSS Components/Menubar"
  component={MenuBar}
  argTypes={argTypes}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Menubar

<Canvas>
  <Story name="Menubar" args={defaultArgs}>
    {MenuBar.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Menubar" />

## Dependencies

@nl-rvo/assets/icons

## Changelog

<Description>{changeLog}</Description>
