import frameworkDecorator from "@nl-rvo/storybook/frameworkDecorator";
import { Canvas, Meta, Story } from "@storybook/addon-docs";
import { argTypes, defaultArgs, MenuBar } from "./template";
import "./index.scss";

<Meta
  title="CSS Components/Menubar/Menu item alignment"
  argTypes={argTypes}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
  decorators={[frameworkDecorator]}
/>

# Menubar item alignment

Align items to the right with the `.rvo-topnav__spacer` classname:

<Canvas>
  <Story name="Menu item alignment" args={defaultArgs}>
    {MenuBar.bind()}
  </Story>
</Canvas>
