import { Canvas, Meta, Story } from "@storybook/addon-docs";
import { argTypes, defaultArgs, MenuBar } from "./template";
import "./index.scss";

<Meta
  title="CSS Components/Menubar/Sizes"
  argTypes={argTypes}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Menubar sizes

### Small

<Canvas>
  <Story name="Small" args={{ ...defaultArgs, showIcons: "before", size: "small" }}>
    {MenuBar.bind()}
  </Story>
</Canvas>

### Medium

<Canvas>
  <Story name="Medium" args={{ ...defaultArgs, showIcons: "before", size: "medium" }}>
    {MenuBar.bind()}
  </Story>
</Canvas>
