import { Canvas, Meta, Story } from "@storybook/blocks";
import { defaultArgs } from "./defaultArgs";
import { argTypes, MenuBar } from "./template";
import * as MenubarActiveStories from "./menubar.active.stories";

<Meta of={MenubarActiveStories} />

# Menubar active item

Make an item active with the `.utrecht-topnav__item--active` classname:

<Canvas>
  <Story of={MenubarActiveStories.MenuActiveItem} />
</Canvas>
