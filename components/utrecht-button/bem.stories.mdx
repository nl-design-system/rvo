<!-- @license CC0-1.0 -->

import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { AllButtonKinds, Button, defaultArgs } from "./bem";
import "./bem.scss";

<Meta
  title="CSS Components/Button (utrecht)"
  argTypes={{
    kind: {
      options: ["primary", "secondary", "tertiary", "quaternary"],
      control: { type: "radio" },
    },
    size: {
      options: ["small", "medium"],
      control: { type: "radio" },
    },
    textContent: {
      description: "Button text",
      control: "text",
    },
    busy: {
      description: "Busy",
      control: "boolean",
    },
    focus: {
      description: "Focus",
      control: "boolean",
    },
    focusVisible: {
      description: "Focus visible",
      control: "boolean",
    },
    hover: {
      description: "Hover",
      control: "boolean",
    },
    disabled: {
      description: "Disabled",
      control: "boolean",
    },
  }}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Button

Styling via the `.utrecht-button` classname:

<Canvas>
  <Story
    name="Button"
    args={{
      ...defaultArgs,
    }}
  >
    {Button.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Button" />

## Kinds

### Primary

Styling via the `.utrecht-button--primary-action` classname:

<Canvas>
  <Story
    name="Button primary"
    args={{
      kind: "primary",
    }}
  >
    {Button.bind({})}
  </Story>
</Canvas>

### Secondary

Styling via the `.utrecht-button--secondary-action` classname:

<Canvas>
  <Story
    name="Button secondary"
    args={{
      kind: "secondary",
    }}
  >
    {Button.bind({})}
  </Story>
</Canvas>

### Tertiary

Styling via the `.rvo-button--tertiary-action` classname:

<Canvas>
  <Story
    name="Button tertiary"
    args={{
      kind: "tertiary",
    }}
  >
    {Button.bind({})}
  </Story>
</Canvas>

### Quaternary

Styling via the `.rvo-button--quaternary-action` classname:

<Canvas>
  <Story
    name="Button quaternary"
    args={{
      kind: "quaternary",
    }}
  >
    {Button.bind({})}
  </Story>
</Canvas>

## Sizes

### Small

Styling via the `.rvo-button--small` classname:

<Canvas>
  <Story
    name="Button small"
    args={{
      kind: "primary",
      size: "small",
    }}
  >
    {AllButtonKinds.bind({})}
  </Story>
</Canvas>

### Medium

Styling via the `.rvo-button--medium` classname:

<Canvas>
  <Story
    name="Button medium"
    args={{
      kind: "primary",
      size: "medium",
    }}
  >
    {AllButtonKinds.bind({})}
  </Story>
</Canvas>

## States

### Active

Styling via the `.utrecht-button--active` classname:

<Canvas>
  <Story
    name="Button active"
    args={{
      active: true,
    }}
  >
    {AllButtonKinds.bind({})}
  </Story>
</Canvas>

### Hover

Styling via the `.utrecht-button--hover` classname:

<Canvas>
  <Story
    name="Button hover"
    args={{
      hover: true,
    }}
  >
    {AllButtonKinds.bind({})}
  </Story>
</Canvas>

### Focus

Styling via the `.utrecht-button--focus` classname:

<Canvas>
  <Story
    name="Button focus"
    args={{
      focus: true,
    }}
  >
    {AllButtonKinds.bind({})}
  </Story>
</Canvas>

### Focus visible

Styling via the `.utrecht-button--focus-visible` classname:

<Canvas>
  <Story
    name="Button focus visible"
    args={{
      focus: true,
      focusVisible: true,
    }}
  >
    {AllButtonKinds.bind({})}
  </Story>
</Canvas>

### Disabled

Styling via the `.utrecht-button--disabled` classname:

<Canvas>
  <Story
    name="Button disabled"
    args={{
      disabled: true,
    }}
  >
    {AllButtonKinds.bind({})}
  </Story>
</Canvas>

### Busy

Styling via the `.utrecht-button--busy` classname:

<Canvas>
  <Story
    name="Button busy"
    args={{
      busy: true,
    }}
  >
    {AllButtonKinds.bind({})}
  </Story>
</Canvas>
