import { Canvas, Description, Meta, Story } from "@storybook/addon-docs";
import changeLog from "./CHANGELOG.md";
import { defaultArgs } from "./defaultArgs";
import { argTypes, FormLayout } from "./template";

<Meta
  title="CSS Components/Layout/Form layout"
  component={FormLayout}
  argTypes={argTypes}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
    docusaurus: {
      link: "form-layout",
    },
  }}
/>

# Form layout

<Canvas>
  <Story name="Form layout" args={defaultArgs}>
    {FormLayout.bind({})}
  </Story>
</Canvas>

## Changelog

<Description>{changeLog}</Description>
