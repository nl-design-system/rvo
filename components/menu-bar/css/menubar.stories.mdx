import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { argTypes, MenuBar, defaultArgs } from "./template";
import "./index.scss";

<Meta
  title="CSS Components/Menu bar"
  argTypes={argTypes}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Menu bar

Menu bar containing items without icons:

<Canvas>
  <Story name="Menu bar without icons" args={defaultArgs}>
    {MenuBar.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Button" />

Menu bar containing items with icons:

<Canvas>
  <Story name="Menu bar with icons" args={{ ...defaultArgs, useIcons: true }}>
    {MenuBar.bind()}
  </Story>
</Canvas>
