import { ArgsTable, Canvas, Description, Meta, Story } from "@storybook/addon-docs";
import changeLog from "./CHANGELOG.md";
import { defaultArgs } from "./defaultArgs";
import { Accordion, argTypes } from "./template";

<Meta
  title="CSS Components/Accordion/Accordion"
  component={Accordion}
  argTypes={argTypes}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Accordion

<Canvas>
  <Story name="Accordion" args={defaultArgs}>
    {Accordion.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Accordion" />

## Changelog

<Description>{changeLog}</Description>
