import { Canvas, Controls, Markdown, Meta, Story, Source, Title } from "@storybook/blocks";
import * as AccordionStories from "./accordion.stories";
import documentation from "./README.md";
import changeLog from "./CHANGELOG.md";

<Meta of={AccordionStories} name="Documentatie" />

<Title />
<Canvas of={AccordionStories.Default} layout="padded" />
<Controls />

## Documentatie

<Markdown>{documentation}</Markdown>

## Changelog

<Markdown>{changeLog}</Markdown>
