import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { defaultArgs } from "./defaultArgs";
import { argTypes, FileInputField } from "./fileinput-field.template";

<Meta
  title="CSS Components/Form/Field/File input field"
  component={FileInputField}
  argTypes={argTypes}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# File input field

<Canvas>
  <Story name="File input field" args={defaultArgs}>
    {FileInputField.bind({})}
  </Story>
</Canvas>

<ArgsTable story="File input field" />
