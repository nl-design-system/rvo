import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { argTypes, defaultArgs, RadioButton } from "./template";

<Meta
  title="CSS Components/Form/Radio button"
  argTypes={argTypes}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Form radio button

<Canvas>
  <Story name="Radio button" args={defaultArgs}>
    {RadioButton.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Radio button" />
