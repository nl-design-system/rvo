import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { argTypes, defaultArgs, RadioButtonGroup } from "./template.group";

<Meta
  title="CSS Components/Form/Radio button/Radio button group"
  argTypes={argTypes}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Form Radio button group

<Canvas args={defaultArgs}>
  <Story name="Radio button group">{RadioButtonGroup.bind({})}</Story>
</Canvas>

<ArgsTable story="Radio button group" />
