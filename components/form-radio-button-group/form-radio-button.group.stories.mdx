import { ArgsTable, Canvas, Description, Meta, Story } from "@storybook/addon-docs";
import changeLog from "./CHANGELOG.md";
import { defaultArgs } from "./defaultArgs";
import { argTypes, RadioButtonGroup } from "./template";

<Meta
  title="CSS Components/Form/Radio button/Radio button group"
  component={RadioButtonGroup}
  argTypes={argTypes}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Radio button group

Let op: Gebruik de radio button group altijd in combinatie met een heldere vraag erboven.

<Canvas>
  <Story name="Radio button group" args={defaultArgs}>
    {RadioButtonGroup.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Radio button group" />

## Changelog

<Description>{changeLog}</Description>
