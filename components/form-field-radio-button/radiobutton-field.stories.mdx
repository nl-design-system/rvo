import { ArgsTable, Canvas, Description, Meta, Story } from "@storybook/addon-docs";
import changeLog from "./CHANGELOG.md";
import { defaultArgs } from "./defaultArgs";
import { argTypes, RadioButtonField } from "./template";

<Meta
  title="Componenten/Form/Field/Radio button field"
  component={RadioButtonField}
  argTypes={argTypes}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
    docusaurus: {
      link: "form-field-radio-button",
    },
  }}
/>

# Radio button field

<Canvas>
  <Story name="Radio button field" args={{ ...defaultArgs, helperText: "Helper text" }}>
    {RadioButtonField.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Radio button field" />

## Changelog

<Description>{changeLog}</Description>
