import { ArgsTable, Canvas, Description, Meta, Story } from "@storybook/addon-docs";
import changeLog from "./CHANGELOG.md";
import { defaultArgs } from "./defaultArgs";
import { argTypes, DateInputField } from "./template";

<Meta
  title="Componenten/Form/Field/Date input field"
  component={DateInputField}
  argTypes={argTypes}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
    docusaurus: {
      link: "form-field-dateinput",
    },
  }}
/>

# Date input field

<Canvas>
  <Story name="Date input field" args={{ ...defaultArgs, helperText: "Helper text" }}>
    {DateInputField.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Date input field" />

## Changelog

<Description>{changeLog}</Description>
