import { ArgsTable, Canvas, Description, Meta, Story } from "@storybook/addon-docs";
import { defaultArgs } from "./defaultArgs";
import { argTypes, MobileMenuBar } from "./template";
import changeLog from "../CHANGELOG.md";

<Meta
  title="CSS Components/Menubar/Mobile"
  component={MobileMenuBar}
  argTypes={argTypes}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Mobile menubar

<Canvas>
  <Story name="Mobile" args={{ ...defaultArgs, isOpen: true }}>
    {MobileMenuBar.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Mobile" />

## Dependencies

@nl-rvo/assets/icons

## Changelog

<Description>{changeLog}</Description>
