/**
 * Storybook-only argTypes copied from React component source.
 */
export const argTypes = {
  steps: {
    type: {
      name: 'array',
      required: true,
    },
  },
  children: {
    table: {
      disable: true,
    },
  },
};
