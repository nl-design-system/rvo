import frameworkDecorator from "@nl-rvo/storybook/frameworkDecorator";
import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { argTypes, defaultArgs, Step } from "./template.step";

<Meta
  title="CSS Components/Progress Tracker"
  argTypes={argTypes}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
  decorators={[frameworkDecorator]}
/>

# Step

<Canvas>
  <Story name="Step" args={defaultArgs}>
    {Step.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Step" />
