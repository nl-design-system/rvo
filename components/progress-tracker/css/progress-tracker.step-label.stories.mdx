import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { argTypes, defaultArgs, StepLabel } from "./template.step-label";

<Meta
  title="CSS Components/Progress Tracker/Elements"
  argTypes={argTypes}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Step label

<Canvas>
  <Story name="Step label" args={defaultArgs}>
    {StepLabel.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Step label" />
