import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { argTypes, defaultArgs, StepIcon } from "./template.step-icon";

<Meta
  title="CSS Components/Progress Tracker/Elements"
  argTypes={argTypes}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Step icon

<Canvas>
  <Story name="Step icon" args={defaultArgs}>
    {StepIcon.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Step icon" />
