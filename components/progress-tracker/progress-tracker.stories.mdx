import { ArgsTable, Canvas, Description, Meta, Story } from "@storybook/addon-docs";
import { defaultArgs } from "./defaultArgs";
import { argTypes, ProgressTracker } from "./template";
import changeLog from "./CHANGELOG.md";

<Meta
  title="CSS Components/Progress Tracker/Progress Tracker"
  component={ProgressTracker}
  argTypes={argTypes}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Progress Tracker

<Canvas>
  <Story name="Progress Tracker" args={defaultArgs}>
    {ProgressTracker.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Progress Tracker" />

## Changelog

<Description>{changeLog}</Description>
