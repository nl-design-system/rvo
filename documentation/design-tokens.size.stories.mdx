import designTokens from "@nl-rvo/design-tokens/dist/index.json";
import { Description, Meta } from "@storybook/addon-docs";
import { SizeDesignTokens } from "./components/SizeDesignTokens";

export const tokens = designTokens.filter(({ filePath }) => filePath.includes("src/brand/rvo/space.tokens.json"));

<Meta title="Algemeen/Design Tokens/Groottes" />

<Description># Groottes</Description>

<p>Er zijn {tokens.length} design tokens voor groottes.</p>

<SizeDesignTokens tokens={tokens} />
