import designTokens from "@nl-rvo/design-tokens/dist/index.json";
import { BorderDesignTokens } from "../../components/design-tokens/BorderDesignTokens";

export const tokens = designTokens.filter(({ filePath }) => filePath.includes("src/brand/rvo/border.tokens.json"));

<span>Er zijn {tokens.length} design tokens voor border-radius.</span>

<BorderDesignTokens tokens={tokens} />
