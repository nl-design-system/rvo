---
"@nl-rvo/component-library-react": minor
---

All icons are now decorative by default (WCAG SC 1.1.1). The `ariaLabel` prop is removed from `Icon` and `iconAriaLabel` is removed from `Button` and `Link`. The icon `<span>` now always renders with `aria-hidden="true"`.
