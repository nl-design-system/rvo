---
"@nl-rvo/component-library-react": patch
---

Dialog close button now uses the default subtle button variant without dialog-specific CSS classes. This restores the button's default padding (`padding-inline-end` and `padding-block-start`), which changes the position of the close button.
